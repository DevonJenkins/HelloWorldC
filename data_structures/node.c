typedef struct node
{
	string phrase;
	struct node *next; 
}

node;

node *n = malloc(sizeof(node));

n->phrase = "Hi!";
n->next = NULL
list = n;

n = malloc(sizeof(node));

n->phrase = "HEY!";
n->next list;
list = n;



//make sure to call free after running malloc to free up the unused or garbage
//data 
//
//


